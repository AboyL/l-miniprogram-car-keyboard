<!-- index.wxml -->
<view class="container">
  <navigator url="/pages/simple/index">前往简单使用</navigator>
  <l-key-input plate="{{plate}}" currentIndex="{{currentIndex}}" bind:indexChange="handleIndexChange"/>
  <l-key-board wx:if="{{showKeyboard}}" id="keyboard" bind:choose="handlePlateChoose" bind:close="handleClose" bind:delete="handlePlateDelete" disabledNumber="{{disabledNumber}}"/>
</view>